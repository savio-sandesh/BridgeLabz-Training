using System;

class CeltoFah{
		static void Main(){
			
		// Temperature in Celsius
		double c=37.5;
		
		// Convert Celsius to Fahrenheit using formula:
        // Fahrenheit = (Celsius × 9 / 5) + 32
		double f=(c*(9/5)) + 32;
		Console.WriteLine(f);
		}
	}